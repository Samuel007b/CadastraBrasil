info:
  name: Alterar cidadão inexistente
  type: http
  seq: 11

http:
  method: PUT
  url: "{{baseUrl}}/api/pessoas/999"
  body:
    type: json
    data: |-
      {
        "nome": "Felipe Moreira",
        "cpf": "008.497.690-00"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 404", function() {
          expect(res.getStatus()).to.equal(404);
        });

        test("deve retornar mensagem de erro", function() {
          expect(res.getBody().erro).to.equal("Cidadão não encontrado.");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
